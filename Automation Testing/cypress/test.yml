config:
  target: "https://postman-echo.com"
  phases:
    - duration: 3
      arrivalRate: 2
      name: Artillery First Test
scenarios:
  - name: Getting a resource
    flow:
      - get:
          url: "{{ target }}/get"

